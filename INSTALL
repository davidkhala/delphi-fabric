# install nodejs
curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -
sudo apt-get -qq install -y nodejs

# balance-transfer example dependencies
sudo apt-get -qq install -y jq

# to use sponge
sudo apt -qq install -y moreutils

