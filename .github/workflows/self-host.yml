name: Self-host runner

on:
  workflow_dispatch:
  push: # testing
jobs:
  e2e:
    runs-on: self-hosted
    steps:
      - uses: davidkhala/ci-cd-utils/.github/actions/ol7.yml@mastaer
      - uses: ./.github/workflows/ci.yml
        with:
          os: self-hosted
