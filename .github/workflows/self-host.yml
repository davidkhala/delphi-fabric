name: Self-host runner

on:
  workflow_dispatch:
  push: # testing
jobs:
  e2e:
    runs-on: self-hosted
    steps:
      - uses: davidkhala/ci-cd-utils/blob/master/.github/actions/ol7.yml@mastaer
      - uses: davidkhala/delphi-fabric/.github/workflows/ci.yml@master
        with:
          os: self-hosted
