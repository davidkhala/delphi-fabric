{
  "name": "delphi-fabric",
  "version": "2.1.0",
  "private": true,
  "description": "node program to use Hyperledger Fabric nodejs SDK ",
  "keywords": [
    "v1.x fabric node-sdk sample"
  ],
  "scripts": {
    "install": "npm run nuke; cd common/nodejs; npm install; lerna bootstrap --no-ci; cd -",
    "nuke": "rm -rf node_modules; cd common/nodejs; rm -rf node_modules; lerna clean --yes; cd -",
    "test": "mocha"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "khala-dockerode": "^0.1.2",
    "khala-nodeutils": "^0.8.2",
    "khala-light-util": "^0.0.3"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "mocha": "^7.2.0",
    "chai": "^4.2.0",
    "khala-logger": "^0.2.0"
  }
}
