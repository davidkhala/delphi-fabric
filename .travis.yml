dist: bionic
os:
  - linux
  - osx
before_install:
  - eval "$(gimme 1.14.x)"
  - ./install.sh
  - ./common/install.sh softHSM
before_script:
  - ./test/hsm.sh
jobs:
  fast_finish: true
  include:
    - language: node_js
      node_js:
        - lts/*
      install:
        - npm install --global mocha
      script:
        - npm start
        - npm test
